navbar(data-hide="{isHide}")

    h1 {title}
    nav.navbar-nav
        .nav-search
            button(onclick="{changeModeSearch}"): span.fa.fa-search
        button(onclick="{openDrawer}"): span.fa.fa-navicon
    
    script(type="es6").
    
